#!/usr/bin/env bash

set -e

# Drivers from host need to be in linker cache
ldconfig

# We initialise MPI over SSH, so launch SSHD
mkdir -p /var/run/sshd
ssh-keygen -A
exec /usr/sbin/sshd -D -p "${SSH_PORT:-22}"
